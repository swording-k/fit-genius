# PRD · FitGenius v1.5 — 能力感知训练处方 + 目标多选

> 文档状态：Draft v1（范围已于 2026-07-20 与产品负责人对齐）
> 负责人：产品通（PM）
> 关联版本：v1.5（独立于 v1.4 云同步版，走新版本 SwiftData 迁移）
> 来源反馈：用户邮件（1 位女性用户，n=1，定性信号，与健身 App 通用最佳实践一致）

---

## 1. Problem Statement（问题陈述）

一位女性新用户反馈："没有考虑男性女性的差异化，我是女生，我一个俯卧撑也撑不起来怎么办"；并建议"目标可以多选，选项还可以增加"。

**根因（PM 视角，需先掰正）**：用户口中的"男女差异"是表象，真正的问题是 **App 生成的是通用模板，完全没有按用户起点做能力校准**。一个连引体向上都做不了的新手男生会遇到完全相同的困境。因此：
- 仅加一个"性别"字段**解决不了问题**——除非同时让 AI 按性别/基线给退阶动作。
- "性别"是有用的**启发信号**（女性上肢推力基线统计上更低），但**能力基线**才是真正的调节杠杆。

**现状佐证（已读代码核实）**：
- `UserProfile` 中**无性别字段**、无训练经验/能力基线字段。
- `UserProfile.goal: FitnessGoal` 是**单值**，数据模型锁死单选。
- 动作库 `ExerciseTemplate` / `exercises_seed.json` 仅有 `equipment`，**无难度/退阶标签**（1324 条均无）。
- AI 提示词（`AIService.profilePrompt`）有"新手/年龄"引导，但**无性别/能力感知、无"按能力给退阶动作"指令**。

**不解决的代价**：女性/新手因首份计划过难而挫败 → 早期流失（健身 App 7 日流失率本就高）；"做不到的动作"类反馈持续；NPS"计划贴合度"偏低；与竞品（如 Keep、Fiton）的能力适配差距扩大。

---

## 2. Goals（目标，须可衡量）

**用户目标**
- G1：新用户能在入档时表达性别与训练经验，使首份计划贴合起点。
- G2：当用户做不到某动作时，AI 自动用退阶动作替代，或用户能在计划内一键换成退阶动作。
- G3：用户能选多个目标，且目标选项覆盖更广的诉求。

**业务目标**
- G4：降低女性/新手早期流失（提升 7 日、30 日留存）。
- G5：降低"计划过难/做不到"类负面反馈与客服量。
- G6：提升 NPS 中"计划贴合度"分项。

---

## 3. Non-Goals（明确不做的，防范围蔓延）

- **NG1 · 不做完整体能测评**（1RM、动作筛查 FMS、最大摄氧量等）。理由：超出本次体验优化范围，工作量大且需专业内容，另立专项。
- **NG2 · 不做女性生理周期/激素训练适配**。理由：隐私与复杂度高，且当前 n=1 未验证该需求；本次仅用性别作为基线启发信号。
- **NG3 · 不强制全量新增动作或新 GIF 资源**。理由：退阶动作一律引用库内现有 1324 条动作（GIF 已存在），不引入新版权素材，避免触碰 App Store GIF 版权硬关。
- **NG4 · 不改 v1.4 已生成旧计划的兼容性以外逻辑**。理由：旧计划保留；新能力仅作用于 v1.5 起新生成/编辑的计划。
- **NG5 · 不引入社交/社区/排行榜**。理由：与本问题无关，避免分散 v1.5 焦点。

**Parking Lot（本次未选但候选的未来目标项）**：塑形/体态改善、产后恢复、增重、运动康复——本期仅新增"减压/睡眠"，其余留作后续版本候选。

---

## 4. User Stories（用户故事）

**US1（采集基线）** — 作为新用户，我希望入档时填写性别和训练经验（新手/进阶/资深），以便 App 给我更贴合起点的计划。
- 边界：选择"不愿填写"时，AI 走中性策略（仍可在用户要求时给退阶），不阻断注册。

**US2（AI 自动退阶）** — 作为女性/新手用户，当我做不到某个动作（如标准俯卧撑）时，我希望 AI 自动用退阶动作（上斜/跪姿俯卧撑）替代，而不是给我一个做不到的计划。
- 边界：若库内无对应退阶动作，AI 应选用同肌群、更低难度的现有动作，或明确说明"暂无可替代，建议降低次数/组数"。

**US3（计划内一键退阶）** — 作为用户，当我在计划里看到做不了的动作时，我希望点一下就能换成它的退阶版本，而不必重新生成整份计划。
- 边界：退阶动作须为库内现有动作；若无退阶，提示并提供"减少组数/次数"替代建议。

**US4（目标多选）** — 作为用户，我希望能同时选择多个健身目标（如减脂+塑形+减压），以便计划符合我的复合诉求。
- 边界：至少选 1 个才能继续；选 ≥2 时 AI 需平衡各目标（如减脂为主、减压为辅的安排）。

**US5（更多目标项）** — 作为用户，我希望目标选项中包含"减压/睡眠"，以便身心类诉求也能被服务。

---

## 5. Requirements（需求，MoSCoW）

### Workstream A · 用户档案模型扩展（P0）

**A1（P0）** `UserProfile` 新增字段：
- `biologicalSex: Sex?`（`.female` / `.male` / `.other` / `nil`=未填）
- `experienceLevel: ExperienceLevel`（`.beginner` / `.intermediate` / `.advanced`，默认 `.beginner`）
- 字段均为可选/有默认，不阻断旧用户。

**A2（P0）** SwiftData 版本迁移（v1.4 → v1.5）：
- 使用 `VersionedSchema` + `MigrationPlan`；旧单值 `goal` → `[FitnessGoal]`（含原值）；`biologicalSex`/`experienceLevel` 对新用户留默认。
- 验收：v1.4 升级到 v1.5 的用户数据不丢、不崩；旧计划可读。

**A3（P0）** 入档 UI（Onboarding）新增两步/合并到 basic_info：性别选择 + 训练经验单选。文案中性、可跳过。

### Workstream B · 目标多选 + 新选项（P0）

**B1（P0）** `FitnessGoal` 新增 `.stressReliefSleep = "减压/睡眠"`；`UserProfile.goal` 改为 `[FitnessGoal]`（非可选数组，至少 1 项）。
**B2（P0）** 入档 goal_setting 步骤改为多选卡片（现有 equipment 多选交互可复用），至少选 1 个才可前进。
**B3（P0）** AI 提示词注入改为多目标列表；当 ≥2 目标时，指令 AI 按优先级平衡（主目标驱动训练分化，次目标以低量有氧/拉伸/呼吸训练承载）。

### Workstream C · 动作库难度/退阶标注（P0，重头活）

**C1（P0）** `exercises_seed.json` 每条新增字段：
- `difficulty: "beginner"|"intermediate"|"advanced"`
- `regressions: [exerciseId]`（更容易的现有动作 id，可空）
- `progressions: [exerciseId]`（更难版本，可空）
- `ExerciseTemplate` 对应新增字段并映射。

**C2（P0）** 标注方式：**AI 批量标注 + 人工抽检**。用现有 AI（MiniMax-M3）读取每条 `name/target/equipment/zh/en`，按规则产出 `difficulty` 与 `regressions`（须引用库内现有 id，禁止造不存在的动作）。产出后人工抽检 ≥5% 并修正明显错误（尤其俯卧撑/引体/深蹲等大类须有清晰退阶链）。
**C3（P0）** 关键动作须保证退阶链完整：俯卧撑→上斜俯卧撑→跪姿俯卧撑→墙壁俯卧撑；引体→反向划船/弹力带引体；深蹲→箱式深蹲→扶椅深蹲等。由人工确保覆盖 Top 50 高频动作。

### Workstream D · AI 处方能力感知（P0）

**D1（P0）** `AIService.profilePrompt` 重写：
- 注入 `biologicalSex`、`experienceLevel`、多目标。
- 新增硬指令：①若用户为女性或新手，自重复合动作（俯卧撑/引体/双杠等）优先给退阶版本；②避免处方用户明显无法完成的动作；③当用户在对话中声明"做不了 X"，改用其退阶或同肌群低难度现有动作；④多目标时平衡安排。
- 验收：用"女生、新手、目标减脂"跑生成，返回计划不得含标准俯卧撑（应含退阶）；含"减压/睡眠"时计划含低量有氧/拉伸/呼吸类。

**D2（P1）** 在 `regeneratePlan` 的 userRequest 通道增强：识别"做不了/太难/换简单"等语义，触发退阶替换而非整份重生成。

### Workstream E · 计划内一键退阶 UI（P1）

**E1（P1）** 计划日动作行新增"换退阶"入口：点击 → 若该动作在库中有 `regressions`，弹出退阶动作列表（名称+GIF 预览）→ 选定后**仅替换该动作名**（保持 sets/reps 由用户或 AI 建议微调）。
**E2（P1）** 若无退阶：提示并提供"减少组数/次数"快捷建议。
**E3（P2）** 退阶替换记录进用户行为（用于度量 E 使用率），不强制云同步。

### 优先级小结
- **P0（必须有）**：A1–A3、B1–B3、C1–C3、D1。缺任一项则核心问题未解。
- **P1（应有）**：D2、E1–E2。显著提升体验，可紧接 fast-follow。
- **P2（未来）**：E3 行为埋点深化；生理周期适配（NG2）；完整测评（NG1）。

---

## 6. Success Metrics（成功指标）

**Leading（上线后数周内）**
- M1 目标多选采用率：v1.5 用户中 ≥30 日内选 ≥2 目标占比 **≥ 35%**（成功线）/ 50%（拉伸）。
- M2 女性新用户激活率（完成首计划且 7 日内打开 ≥3 次）较 v1.4 同口径 **+15%**。
- M3 "做不到的动作"类反馈/客服量上线 30 日内 **−40%**。
- M4 计划内退阶替换使用率：上线首月 **≥ 20%** 用户至少用过一次（E1）。

**Lagging（数月至一季）**
- M5 女性用户 30 日留存 **+10pp**。
- M6 NPS"计划贴合度"分项 **≥ 4.3 / 5**。
- M7 因"计划过难"导致的早期卸载率下降（归因：退阶使用高 + 留存升）。

**测量方式**：客户端行为埋点（目标选择、退阶替换点击、计划生成后手动删/换动作比例作为"不适配"代理）+ 应用内反馈/NPS 问卷 + 客服工单分类。

---

## 7. Acceptance Criteria（验收，Given/When/Then 节选）

- **AC-A** Given 升级用户（v1.4→v1.5），When 打开 App，Then 旧资料与计划完整可读，无崩溃，无数据丢失。
- **AC-B** Given 新用户，When 在入档选性别=女 + 经验=新手 + 目标=[减脂, 减压/睡眠]，Then 生成的计划：不含标准俯卧撑等自重高难动作；含低量有氧/拉伸承载"减压/睡眠"；多目标被平衡。
- **AC-C** Given 用户在计划内看到"标准俯卧撑"，When 点"换退阶"，Then 弹出退阶列表（上斜/跪姿/墙壁），选定后该动作被替换且计划其余部分不变。
- **AC-D** Given 某动作在库中 `regressions` 为空，When 用户点"换退阶"，Then 提示并提供"减少组数/次数"建议，而非报错或空列表。
- **AC-E** Given 用户仅选 1 个目标，When 前进，Then 正常生成；Given 选 0 个，Then 禁止前进并提示至少选 1 个。

---

## 8. Open Questions（待决问题）

**Blocking（开工前必须答）**
- OQ1（工程）：v1.4→v1.5 迁移是否采用 `MigrationPlan`，还是借 v1.5 作为"新安装为主"的版本、对升级用户做一次性兼容映射？— 待工程评估。
- OQ2（数据）：AI 批量标注 1324 条的准确率验收阈值与抽检人力谁出？— 待 PM+工程+数据对齐（建议抽检 ≥5%，Top 50 高频动作 100% 人工）。
- OQ3（设计）：性别字段文案与"不愿填写"的中性表达，是否通过审阅避免刻板印象？— 待设计/用研。

**Non-blocking（实现中可解）**
- OQ4：多目标平衡的指派规则（主/次目标权重）是否需要可配置？先硬编码"首目标主导"。
- OQ5：退阶替换是否写入云同步快照？先本地，不强制上云（见 E3）。

---

## 9. Timeline / Phasing（v1.5 分期）

建议分两波，避免与 v1.4 云同步互相拖累：
- **Wave 1（P0，核心）**：A 系列（模型+迁移+UI）、B 系列（多选+减压睡眠）、C 系列（标注）、D1（AI 感知）。→ 决定"能力适配"是否成立。
- **Wave 2（P1，fast-follow）**：D2、E 系列（一键退阶 UI）。→ 体验闭环。
- 依赖：C 系列标注是 Wave 1 关键路径，需最早启动；D1 依赖 C 完成才有退阶可引用。

**硬依赖/风险**：
- SwiftData 迁移若处理不当会导致升级用户数据损坏（最高风险，需工程先验证）。
- AI 标注质量直接决定退阶可用性，须人工守住 Top 50。
- v1.5 独立于 v1.4，但共享同一 CloudBase 后端与动作库种子，部署时需同步种子文件。

---

## 10. 风险与合规

- **R1 迁移风险（高）**：见 OQ1/AC-A，必须先在模拟器与真机验证升级路径。
- **R2 标注质量风险（中）**：AI 误标难度/错误退阶链会直接伤害用户；以人工抽检 + Top 50 全覆盖兜底。
- **R3 隐私（低-中）**：性别为敏感个人信息，须写入隐私政策（与 v1.4 云同步的 CloudBase PII 声明一并补齐），默认可跳过、不强制。
- **R4 版权（低）**：退阶动作均为库内现有动作，GIF 已授权（© Gym visual），不新增素材，不触发新版权风险。
- **R5 范围蔓延（中）**：严格守住 Non-Goals；任何新增须伴随移除或延期。

---

*附：本期仅新增目标项"减压/睡眠"；塑形/体态改善、产后恢复、增重、运动康复列为 Parking Lot，待数据验证后再纳入。*
