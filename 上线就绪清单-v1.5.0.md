# FitGenius v1.5.0 上线就绪确认

> 生成时间：2026-07-20 | 状态：代码已推送 GitHub，隐私政策部署中，待你本地验证 + App Store 提审

## ✅ 本次会话已完成（服务端 + 客户端 + 文档）

| 事项 | 状态 | 说明 |
|------|------|------|
| 隐私政策重写（根目录 + `docs/` 两份） | ✅ | 修正旧版"不收集/不传服务器/阿里云"谎言 → 改为 CloudBase 云同步 + MiniMax + 可选登录 + 性别 PII + 直连模式 |
| v1.5 体验优化合并进 `main` | ✅ | 多选目标 + 性别/经验采集 + AI 退阶指令（纯加法字段，老用户升级零迁移风险） |
| 版本号 bump → 1.5.0 (build 20260720) | ✅ | 已提交 `main` |
| README 同步 | ✅ | 徽章 1.4.0→1.5.0；版本历史新增 v1.5.0 节 |
| 推送 GitHub `main` + 打 `v1.5.0` 标签 | ✅ | `920afba..a646c3f main -> main`，`[new tag] v1.5.0`；旧标签冲突被拒可忽略 |

## ✅ 后端能力（已实测，用户能享受到的）

- **AI API 接入**：`/api/ai/chat` → CloudBase 云函数 → MiniMax-M3（已修 `reasoning_split` 崩溃）
- **同账号云同步**：`/api/form-analyses`（动作分析）+ `/api/cloud-snapshot`（全量训练快照），按 `userId` **严格隔离**（实测跨用户返回 404）
- **定制化验证明**：模拟"女 + 新手 + 目标[减脂, 减压/睡眠]" → AI 返回**墙壁/上斜/跪姿俯卧撑 + 反向划船**（零标准俯卧撑）+ 腹式深呼吸/瑜伽婴儿式（减压睡眠承载）。痛点真解决，非"感觉"。

## 🚧 待你执行（上线前必做，我替代不了）

1. **真机升级验证（T47）**：若手机装过旧版（v1.2/v1.4），升到 1.5 后旧计划/资料须**不丢、不崩**。纯加法字段理论零风险，但真机必须你验。
2. **等 GitHub Pages 重建（1–2 分钟）**：刷新 `https://swording-k.github.io/fit-genius/privacy-policy.html`，确认看到"腾讯云开发 CloudBase + MiniMax + 登录可选 + 性别 PII"新版。
3. **App Store Connect 提审**：
   - 版本号 `1.5.0`、构建号 `20260720`
   - 隐私政策 URL 填上面的 GitHub Pages 链接
   - 提交审核

## ⚠️ 已知限制（非阻断，后续版本处理）

- **大账号云同步 100KB 限制**：老用户数据多时 `cloud-snapshot` PUT 可能失败（JSON 文本通道 ~100KB 硬限）。彻底解法走 Storage-first 改造。
- **GIF 版权（© Gym visual）**：v1.2 已带、非本次新增、不挡上架；属长期债，建议后续换 CC0/自有资源。
- **崩溃监控缺失**：上线无 Crashlytics，用户崩了你不知。建议下版加。

## 📌 云端数据最小化说明

- 云同步仅上传 `goals`（目标列表），**不上传性别 `biologicalSex`**，刻意压低 PII 风险。
- 登录为可选（可跳过 / 直连模式），Apple 登录符合 Guideline 4.8。
