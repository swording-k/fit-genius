{
  "name": "fitgenius",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test:backend": "node --test backend/tests/*.test.mjs",
    "db:migrate": "node backend/migrate.mjs",
    "check:localization": "scripts/check-localization.sh"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.9.0",
    "jose": "^5.6.0"
  }
}
